---
apiVersion: v1
kind: Namespace
metadata:
  name: keycloak
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: hypermute-contour
  namespace: keycloak
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: hypermute-envoy
  namespace: keycloak
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: hypermute-contour-certgen
  namespace: keycloak
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: hypermute-contour
  namespace: keycloak
data:
  contour.yaml: |
    ingress-status-address: 10.22.1.245
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: hypermute-contour-certgen
  namespace: keycloak
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["create", "update", "patch", "get", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: hypermute-contour-certgen
  namespace: keycloak
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: hypermute-contour-certgen
subjects:
  - kind: ServiceAccount
    name: hypermute-contour-certgen
    namespace: keycloak
---
apiVersion: batch/v1
kind: Job
metadata:
  name: hypermute-contour-certgen-v1-33-2
  namespace: keycloak
spec:
  template:
    metadata:
      labels:
        app: hypermute-contour-certgen
    spec:
      restartPolicy: Never
      serviceAccountName: hypermute-contour-certgen
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        runAsGroup: 65534
      containers:
        - name: contour
          image: altregistry.dev.kube:8443/library/bitnami/contour:1.32.1-debian-12-r0
          imagePullPolicy: IfNotPresent
          command:
            - contour
            - certgen
            - --kube
            - --incluster
            - --overwrite
            - --secrets-format=compact
            - --namespace=$(CONTOUR_NAMESPACE)
          env:
            - name: CONTOUR_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
  parallelism: 1
  completions: 1
  backoffLimit: 1
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: hypermute-contour
rules:
  - apiGroups: [""]
    resources: ["configmaps", "namespaces", "secrets", "services"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["discovery.k8s.io"]
    resources: ["endpointslices"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["gateway.networking.k8s.io"]
    resources: ["backendtlspolicies", "gatewayclasses", "gateways", "grpcroutes", "httproutes", "referencegrants", "tcproutes", "tlsroutes"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["gateway.networking.k8s.io"]
    resources: ["backendtlspolicies/status", "gatewayclasses/status", "gateways/status", "grpcroutes/status", "httproutes/status", "tcproutes/status", "tlsroutes/status"]
    verbs: ["update"]
  - apiGroups: ["networking.k8s.io"]
    resources: ["ingresses"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["networking.k8s.io"]
    resources: ["ingresses/status"]
    verbs: ["create", "get", "update"]
  - apiGroups: ["projectcontour.io"]
    resources: ["contourconfigurations", "extensionservices", "httpproxies", "tlscertificatedelegations"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["projectcontour.io"]
    resources: ["contourconfigurations/status", "extensionservices/status", "httpproxies/status"]
    verbs: ["create", "get", "update"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: hypermute-contour
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: hypermute-contour
subjects:
  - kind: ServiceAccount
    name: hypermute-contour
    namespace: keycloak
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: hypermute-contour
  namespace: keycloak
rules:
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["create", "get", "update"]
  - apiGroups: ["coordination.k8s.io"]
    resources: ["leases"]
    verbs: ["create", "get", "update"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: hypermute-contour
  namespace: keycloak
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: hypermute-contour
subjects:
  - kind: ServiceAccount
    name: hypermute-contour
    namespace: keycloak
---
apiVersion: v1
kind: Service
metadata:
  name: hypermute-contour
  namespace: keycloak
spec:
  type: ClusterIP
  selector:
    app: hypermute-contour
  ports:
    - name: xds
      port: 8001
      targetPort: 8001
      protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  name: hypermute-envoy
  namespace: keycloak
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-backend-protocol: tcp
spec:
  type: LoadBalancer
  loadBalancerIP: 10.22.1.245
  externalTrafficPolicy: Local
  selector:
    app: hypermute-envoy
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
    - name: https
      port: 443
      targetPort: 8443
      protocol: TCP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hypermute-contour
  namespace: keycloak
  labels:
    app: hypermute-contour
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 50%
  selector:
    matchLabels:
      app: hypermute-contour
  template:
    metadata:
      labels:
        app: hypermute-contour
    spec:
      serviceAccountName: hypermute-contour
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        runAsGroup: 65534
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                topologyKey: kubernetes.io/hostname
                labelSelector:
                  matchLabels:
                    app: hypermute-contour
      containers:
        - name: contour
          image: altregistry.dev.kube:8443/library/bitnami/contour:1.32.1-debian-12-r0
          imagePullPolicy: IfNotPresent
          command: ["contour"]
          args:
            - serve
            - --incluster
            - --xds-address=0.0.0.0
            - --xds-port=8001
            - --contour-cafile=/certs/ca.crt
            - --contour-cert-file=/certs/tls.crt
            - --contour-key-file=/certs/tls.key
            - --config-path=/config/contour.yaml
            - --ingress-class-name=hypermute
          env:
            - name: CONTOUR_NAMESPACE
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: metadata.name
          ports:
            - name: xds
              containerPort: 8001
              protocol: TCP
            - name: metrics
              containerPort: 8000
              protocol: TCP
            - name: debug
              containerPort: 6060
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
          readinessProbe:
            tcpSocket:
              port: 8001
            periodSeconds: 10
          volumeMounts:
            - name: contourcert
              mountPath: /certs
              readOnly: true
            - name: contour-config
              mountPath: /config
              readOnly: true
      volumes:
        - name: contourcert
          secret:
            secretName: contourcert
        - name: contour-config
          configMap:
            name: hypermute-contour
            defaultMode: 0644
            items:
              - key: contour.yaml
                path: contour.yaml
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: hypermute-envoy
  namespace: keycloak
  labels:
    app: hypermute-envoy
spec:
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 10%
  selector:
    matchLabels:
      app: hypermute-envoy
  template:
    metadata:
      labels:
        app: hypermute-envoy
    spec:
      serviceAccountName: hypermute-envoy
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 300
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        runAsGroup: 65534
      initContainers:
        - name: envoy-initconfig
          image: altregistry.dev.kube:8443/library/bitnami/contour:1.32.1-debian-12-r0
          imagePullPolicy: IfNotPresent
          command:
            - contour
          args:
            - bootstrap
            - /config/envoy.json
            - --xds-address=hypermute-contour
            - --xds-port=8001
            - --xds-resource-version=v3
            - --resources-dir=/config/resources
            - --envoy-cafile=/certs/ca.crt
            - --envoy-cert-file=/certs/tls.crt
            - --envoy-key-file=/certs/tls.key
          env:
            - name: CONTOUR_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          volumeMounts:
            - name: envoy-config
              mountPath: /config
            - name: envoycert
              mountPath: /certs
              readOnly: true
      containers:
        - name: shutdown-manager
          image: altregistry.dev.kube:8443/library/bitnami/contour:1.32.1-debian-12-r0
          imagePullPolicy: IfNotPresent
          command:
            - /bin/contour
          args:
            - envoy
            - shutdown-manager
          lifecycle:
            preStop:
              exec:
                command:
                  - /bin/contour
                  - envoy
                  - shutdown
          volumeMounts:
            - name: envoy-admin
              mountPath: /admin
        - name: envoy
          image: altregistry.dev.kube:8443/library/bitnami/envoy:1.34.5-debian-12-r0
          imagePullPolicy: IfNotPresent
          command:
            - envoy
          args:
            - -c
            - /config/envoy.json
            - --service-cluster
            - $(CONTOUR_NAMESPACE)
            - --service-node
            - $(ENVOY_POD_NAME)
            - --log-level
            - info
          env:
            - name: CONTOUR_NAMESPACE
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: metadata.namespace
            - name: ENVOY_POD_NAME
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: metadata.name
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
            - name: https
              containerPort: 8443
              protocol: TCP
            - name: metrics
              containerPort: 8002
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /ready
              port: 8002
            initialDelaySeconds: 3
            periodSeconds: 4
          lifecycle:
            preStop:
              httpGet:
                path: /shutdown
                port: 8090
                scheme: HTTP
          volumeMounts:
            - name: envoy-config
              mountPath: /config
              readOnly: true
            - name: envoycert
              mountPath: /certs
              readOnly: true
            - name: envoy-admin
              mountPath: /admin
      volumes:
        - name: envoy-admin
          emptyDir: {}
        - name: envoy-config
          emptyDir: {}
        - name: envoycert
          secret:
            secretName: envoycert
---
apiVersion: networking.k8s.io/v1
kind: IngressClass
metadata:
  name: hypermute
spec:
  controller: projectcontour.io/hypermute-contour
