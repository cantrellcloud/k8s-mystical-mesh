---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.3
  name: cbsvolumebackups.protect.trident.netapp.io
spec:
  group: protect.trident.netapp.io
  names:
    kind: CBSVolumeBackup
    listKind: CBSVolumeBackupList
    plural: cbsvolumebackups
    singular: cbsvolumebackup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.error
      name: Error
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: CBSVolumeBackup is the Schema for the cbsvolumebackups API (only
          available with NetApp Backup and Recovery service)
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CBSVolumeBackupSpec defines the desired state of CBSVolumeBackup
            properties:
              appVaultRef:
                description: AppVaultRef is the reference to the AppVault that this
                  backup belongs to
                minLength: 1
                type: string
              applicationRef:
                description: ApplicationRef is the name of the application that this
                  backup belongs to
                minLength: 1
                type: string
              pvcName:
                description: PVCName is the name of the PVC to backup
                type: string
              pvcNamespace:
                description: PVCNamespace is the namespace of the PVC to backup
                type: string
              reclaimPolicy:
                default: Retain
                description: |-
                  ReclaimPolicy defines what happens to the CBS snaphot/backup when the CBSVolumeBackup CR is deleted
                  Valid options are Retain, Delete (default).
                enum:
                - Retain
                - Delete
                type: string
              snapshotName:
                description: SnapshotName is the name of the snapshot to use for the
                  backup
                type: string
            required:
            - appVaultRef
            - applicationRef
            - pvcName
            - pvcNamespace
            - snapshotName
            type: object
          status:
            description: CBSVolumeBackupStatus defines the observed state of CBSVolumeBackup
            properties:
              aggregates:
                description: Aggregates is a list of the aggregate that the CBS volume
                  belongs to
                items:
                  type: string
                type: array
              backupJobID:
                description: BackupJobID is the ID of the CBS backup job
                type: string
              backupJobRetryCount:
                description: BackupJobRetryCount is the number of retries for the
                  backup job
                type: integer
              bucketName:
                description: BucketName is the name of the bucket where the backup
                  is stored
                type: string
              cbsVolumeID:
                description: CBSVolumeID is the ID of the CBS volume
                type: string
              cbsVolumeName:
                description: CBSVolumeName is the name of the CBS volume
                type: string
              completionTimestamp:
                description: CompletionTimestamp records the time the CBSVolumeBackup
                  completed.
                format: date-time
                nullable: true
                type: string
              conditions:
                description: Each Condition contains details for one aspect of the
                  current state of the CBSVolumeBackup.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              error:
                description: Error indicates the most recent error that has occurred.
                  The error may not be permanent, so progress may continue after temporarily
                  seeing an error.
                type: string
              nextRetryTime:
                description: NextRetryTime records the time when the next retry of
                  the CBSVolumeBackup will be attempted.
                format: date-time
                nullable: true
                type: string
              progress:
                description: Progress is the current progress of this backup as reported
                  by CBS.
                properties:
                  bytesCompleted:
                    description: The current number of bytes moved.
                    format: int64
                    type: integer
                  bytesRemaining:
                    description: The number of bytes yet to be moved.
                    format: int64
                    type: integer
                  bytesTotal:
                    description: The total number of bytes to move.
                    format: int64
                    type: integer
                  updatedAt:
                    description: |-
                      A field only updated when progress is being made. This should be updated
                      when `BytesCompleted` has increased.
                    format: date-time
                    type: string
                required:
                - bytesCompleted
                type: object
              pvName:
                description: PVName is the name of the PV getting backed up
                type: string
              relationshipID:
                description: RelationshipID is the ID of the SnapMirror cloud relationship
                type: string
              snapshotID:
                description: SnapshotID is the ID of the snapshot created for this
                  backup
                type: string
              state:
                description: State indicates a high level summary of the CBSVolumeBackup's
                  state
                type: string
              svm:
                description: Svm is the name of the storage virtual machine that the
                  CBS volume belongs to
                type: string
              systemID:
                description: systemID is the ID of the system that the PV belongs
                  to
                type: string
              systemName:
                description: SystemName is the name of the system that the PV belongs
                  to
                type: string
            required:
            - state
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
